import cv2

img = cv2.imread("image.jpg")

watermark = img.copy()

cv2.putText(watermark,"WATERMARK",(50,50),
            cv2.FONT_HERSHEY_SIMPLEX,
            1,(255,255,255),2)

cv2.imshow("Original Image", img)
cv2.imshow("Watermarked Output", watermark)

cv2.waitKey(0)
cv2.destroyAllWindows()
